# Requirements Traceability

## Prompt 3 traceability summary

| Requirement | Status | Primary evidence |
|---|---|---|
| P3-18 dashboard, read models, OIDC/RBAC, realtime | LOCALLY_VERIFIED | `dashboard/`, migration 0009, `mirage_common.dashboard`, Prompt 3 API, dashboard unit/Playwright and dashboard integration tests |
| P3-18 2D graph and evidence pivots | LOCALLY_VERIFIED | `Graph2D.tsx`, canonical `graph.ts`, 2D/parity suites, browser pivots |
| P3-19 3D relationship graph/parity | LOCALLY_VERIFIED | `Graph3D.tsx`, shared canonical graph, 3D/parity suites, browser interactions |
| P3-20 classified multi-format reports | LOCALLY_VERIFIED | migration 0010, `mirage_common.reports`, reporter worker, report API/UI, report unit/integration tests |
| P3-20 report lifecycle and secure downloads | LOCALLY_VERIFIED | idempotency/progress/cancel/timeout/verify routes and hashed single-use token tests |
| P3-21 server installer/release/SBOM | LOCALLY_VERIFIED for automation/package/non-mutating Ubuntu preflight; LAB for mutating clean-host lifecycle | server installer/release libraries, protected recipes, signed install-report verifier, 14 installer tests, release scripts/runbooks, fresh Ubuntu 24.04 preflight |
| P3-21 endpoint and sandbox packages | CODE_COMPLETE; LAB_VERIFICATION_REQUIRED | hardened WiX/PowerShell/static tests; no Windows compilation or Authenticode result |
| P3-22 thirteen failures | LOCALLY_VERIFIED safety oracle; LAB for injected components | resilience catalogue/runner, 13 recipe entries, failure tests |
| P3-22 31 security checks | LOCALLY_VERIFIED automation; LAB penetration/sign-off | `security/`, security tests, threat model and sign-off |
| P3-22 load | LOCALLY_VERIFIED reduced; LAB Profile B | load harness/tests and `load-results.json`; 1,000 eps deployed run not executed |
| P3-22A observability/alerts | LOCALLY_VERIFIED structure; LAB timing | 40-name OTel catalogue, collector, 13 alert rules, Elastic dashboard, observability tests |
| P3-23 teardown | LOCALLY_VERIFIED local; LAB AWS/Windows | exact 25 steps, scripts/adapters, teardown tests, exact tag inventory |
| P3-24 acceptance | LOCALLY_VERIFIED local; LAB Profile B twice | 25 targets, 36 steps, scripts, signed package/verifier, strict two-run result/package/teardown/reprovision gate, acceptance tests/runbooks |

No row above has status `ACCEPTED`. Profile B measurements are represented as
`NOT_RUN`, not inferred from local evidence. The signed local package has 71
total `NOT_RUN` rows: 10 local synthetic numeric plus 61 Profile B (25 numeric
and 36 scenario).

Every requirement below traces to a specification section (spec =
`Mirage_Complete_Engineering_Specification.docx`) or, where explicitly noted, to
a rule stated only in the Prompt 1 instructions (e.g. control-file requirements).
Populated incrementally as each step is implemented — rows are added when work on
that step begins (`IN_PROGRESS`) and updated as evidence accrues.

Columns: **ID · Spec Section · Requirement · Implementation Files · Test Files ·
Status · Evidence · Lab Dependency**

Statuses: `NOT_STARTED` · `IN_PROGRESS` · `CODE_COMPLETE` · `LOCALLY_VERIFIED` ·
`LAB_VERIFICATION_REQUIRED` · `ACCEPTED` · `BLOCKED`

`ACCEPTED` is used only when the spec's own numbered "✓ Done when" acceptance
line for that step has been executed and passed, with evidence recorded in
`TEST_RESULTS.md`.

---

## Bootstrap Gate (BOOT)

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| BOOT-001 | Prompt-1 instructions | `.env.example` documents every configuration variable, no secret values | `.env.example` | `scripts/validate-config --scan-secrets` | LOCALLY_VERIFIED | TEST_RESULTS.md §Bootstrap | No |
| BOOT-002 | Prompt-1 instructions | `config/schema.json` defines required/optional config keys with types | `config/schema.json` | `tests/unit/test_config_schema.py` | LOCALLY_VERIFIED | 13/13 tests pass | No |
| BOOT-003 | Prompt-1 instructions | Per-environment example configs (development/acceptance/production) validate against schema | `config/*.example.yaml` | `tests/unit/test_config_schema.py::test_example_configs_validate_against_schema` | LOCALLY_VERIFIED | passes (3 params) | No |
| BOOT-004 | Prompt-1 instructions | `scripts/validate-config` rejects missing mandatory values | `scripts/validate-config` | `tests/unit/test_config_schema.py::test_schema_rejects_missing_mandatory_section` | LOCALLY_VERIFIED | passes | No |
| BOOT-005 | Prompt-1 instructions | `scripts/check-prerequisites` verifies local tool availability | `scripts/check-prerequisites` | manual run | LOCALLY_VERIFIED | TEST_RESULTS.md §Bootstrap | No |
| BOOT-006 | Prompt-1 instructions | `scripts/doctor` reports tools, versions, ports, missing deps | `scripts/doctor` | manual run | LOCALLY_VERIFIED | TEST_RESULTS.md §Bootstrap | No |
| BOOT-007 | Prompt-1 instructions | `scripts/bootstrap-development` brings up a working dev Compose stack | `scripts/bootstrap-development`, `infra/compose/docker-compose.yml` | manual run | LOCALLY_VERIFIED | 5/5 containers healthy, TEST_RESULTS.md §Bootstrap | No |
| BOOT-008 | Prompt-1 instructions | `scripts/bootstrap-acceptance` documents/validates acceptance (Profile B / AWS) bootstrap | `scripts/bootstrap-acceptance` | manual run | CODE_COMPLETE | exits 1 by design, prints lab steps; full acceptance run is lab-only | Yes — AWS |
| BOOT-009 | Prompt-1 instructions | Secrets Manager catalogue (names + JSON schema) documented for all 9 secret families | `docs/runbooks/secrets.md`, `config/schema.json` | review | LOCALLY_VERIFIED | doc covers all 9 families + rotation | No (doc) |
| BOOT-010 | Prompt-1 instructions | No secret values committed to git | `.gitignore`, repo scan | `scripts/validate-config --scan-secrets`, `tests/unit/test_config_schema.py::test_cli_scan_secrets_*` | LOCALLY_VERIFIED | scan OK on real tree; detection verified against planted fake secret | No |
| BOOT-011 | Prompt-1 instructions | `docs/runbooks/bootstrap.md` exact local + AWS prerequisites | `docs/runbooks/bootstrap.md` | review | LOCALLY_VERIFIED | doc complete | No |

## Stage 0 / Step 1 — Contracts & schema versioning

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S0-1-001 | §Appendix C, Step 1 | Event envelope schema with all specified fields | `schemas/events/envelope.schema.json` | `tests/contract/test_fixtures.py`, `test_generated_agreement.py` | LOCALLY_VERIFIED | 34/34 contract tests pass | No |
| S0-1-002 | §Appendix C, Step 1 | Command envelope schema with all specified fields | `schemas/commands/envelope.schema.json` | `tests/contract/test_fixtures.py` | LOCALLY_VERIFIED | passes | No |
| S0-1-003 | Step 1 | API error envelope schema | `schemas/api/error.schema.json` | `test_generated_agreement.py` | LOCALLY_VERIFIED | generated both sides | No |
| S0-1-004 | Prompt-1 instructions | Agent heartbeat / enrolment schemas | `schemas/events/agent.heartbeat.v1...`, `agent.enrolled.v1...`, `agent.enrollment_failed.v1...`, `agent.enrollment_revoked.v1...`, `agent.certificate_renewed.v1...`, `schemas/api/enroll_request.v1...`, `enroll_response.v1...` | contract + generated-agreement tests | LOCALLY_VERIFIED | 6 heartbeat/enrolment schemas, all generate cleanly | No |
| S0-1-005 | Prompt-1 instructions | Case lifecycle / detection / steering / sandbox command+result / audit / health event schemas | `schemas/events/case.created.v1...`, `case.state_changed.v1...`, `detection.raised.v1...`, `steering.decision_recorded.v1...`, `sandbox.command_result.v1...`, `audit.recorded.v1...`, `system.health.v1...` | contract + generated-agreement tests | LOCALLY_VERIFIED | 7 schemas, all generate cleanly | No |
| S0-1-006 | Step 1 | ULIDs canonical uppercase; validated (Python + TS, matching regex) | `contracts/python/mirage_contracts/ulid.py`, `contracts/typescript/src/ulid.ts` | `tests/contract/test_fixtures.py::test_event_fixture[invalid_id-...]`, TS `generateUlid produces canonical uppercase ULIDs` | LOCALLY_VERIFIED | passes both languages | No |
| S0-1-007 | Step 1 | RFC3339 UTC w/ milliseconds timestamps validated | `contracts/python/mirage_contracts/timestamps.py`, `contracts/typescript/src/timestamps.ts` | `test_fixtures.py::test_event_fixture[invalid_timestamp-...]` | LOCALLY_VERIFIED | passes | No |
| S0-1-008 | Step 1 | Event payload capped at 256 KB; artifact bytes never embedded | `contracts/python/mirage_contracts/envelope.py`, `contracts/typescript/src/envelope.ts` | `test_fixtures.py::test_event_fixture[oversized_payload-...]`, TS `rejects oversized payload` | LOCALLY_VERIFIED | passes both languages | No |
| S0-1-009 | Step 1 | Unknown fields rejected (`additionalProperties: false`) | all 18 schema files | `test_fixtures.py::test_event_fixture[unknown_field-...]`, TS `rejects unknown envelope field` | LOCALLY_VERIFIED | passes | No |
| S0-1-010 | Step 1 | Unsupported major `schema_version` produces typed rejection | `contracts/python/mirage_contracts/registry.py` (`parse_schema_version`), `envelope.py`; TS `registry.ts`/`envelope.ts` | `test_fixtures.py::test_event_fixture[unsupported_version-...]`, TS `rejects unsupported major schema_version` | LOCALLY_VERIFIED | raises `UnsupportedSchemaVersionError`, never enters NATS/Elastic (checked before publish in downstream services) | No |
| S0-1-011 | Step 1 | Generated Pydantic models from JSON Schema (datamodel-code-generator) | `contracts/python/mirage_contracts/generated/*.py` (18 modules) | `test_generated_agreement.py::test_every_schema_has_a_generated_python_model` | LOCALLY_VERIFIED | 18/18 generated, importable, additionalProperties enforced (smoke-tested) | No |
| S0-1-012 | Step 1 | Generated TypeScript types from JSON Schema (json-schema-to-typescript) | `contracts/typescript/src/generated/*.ts` (18 modules) | `test_generated_agreement.py::test_every_schema_has_a_generated_typescript_module`, `tsc` build | LOCALLY_VERIFIED | 18/18 generated, `npm run build` clean | No |
| S0-1-013 | Step 1 | Golden fixtures: valid current, valid previous, invalid ID, invalid timestamp, unknown field, oversized payload, unsupported version, missing required field | `fixtures/events/agent.heartbeat/*.json` (9, incl. bonus integrity_mismatch), `fixtures/commands/sandbox.command/*.json` (3) | `tests/contract/test_fixtures.py` | LOCALLY_VERIFIED | all 8 required kinds present + verified (`test_all_required_fixture_kinds_present`) | No |
| S0-1-014 | Step 1 | `make generate-contracts` / `validate-contracts` / `test-contracts` | `Makefile`, `scripts/generate-contracts`, `scripts/validate-contracts` | manual run of all three | LOCALLY_VERIFIED | TEST_RESULTS.md §Step1 | No |
| S0-1-015 | Step 1 ✓Done | A versioned event round-trips through generated types | `contracts/python/mirage_contracts/generated/events_agent_heartbeat_v1.py` | `test_fixtures.py::test_event_fixture[valid_current]`, `[valid_previous]` | LOCALLY_VERIFIED | passes | No |
| S0-1-016 | Step 1 ✓Done | Unsupported schema_version rejected, not silently accepted | `contracts/python/mirage_contracts/envelope.py` | `test_fixtures.py::test_event_fixture[unsupported_version]` | LOCALLY_VERIFIED | passes, raises typed `UnsupportedSchemaVersionError` | No |
| S0-1-017 | Prompt-1 instructions (CI check) | CI fails when generated types differ from schemas | `scripts/validate-contracts` | manual run: demonstrated by injecting a drift and observing failure, then reverting | LOCALLY_VERIFIED | see TEST_RESULTS.md §Step1 — real drift injected and caught | No |
| S0-1-018 | Prompt-1 instructions (CI check) | CI fails on breaking schema change without version bump | `scripts/validate-contracts::check_no_breaking_change_without_version_bump` | same script, git-history diff based | CODE_COMPLETE | logic present; needs a real git history (commits) to exercise for real — see KNOWN_ISSUES.md | No |
| S0-1-019 | Step 1 (bonus, beyond brief) | `integrity.sha256` verified against payload at validation time | `envelope.py`/`envelope.ts` `IntegrityMismatchError` | `test_fixtures.py::test_event_fixture[integrity_mismatch]` | LOCALLY_VERIFIED | passes | No |

## Stage 0 / Step 1b — NATS JetStream

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S0-1B-001 | Appendix D | Six streams defined with subjects/retention/replicas | `libs/mirage_common/subjects.py` (`STREAM_DEFINITIONS`), `libs/mirage_common/nats_client.py::ensure_streams` | `tests/unit/test_subjects.py`, `tests/integration/test_nats_streams.py::test_production_streams_provision_idempotently` | LOCALLY_VERIFIED | 6/6 streams provisioned against real container + real dev stack (`scripts/provision-nats-streams` output in TEST_RESULTS.md) | No |
| S0-1B-002 | Appendix D, §6.3 | `event_id` used as `Nats-Msg-Id`; dedup window > max retry horizon | `libs/mirage_common/nats_client.py::publish_event`, `subjects.py` (DEDUP_WINDOW_SECONDS=7200 > MAX_RETRY_HORIZON_SECONDS=781) | `test_subjects.py::test_dedup_window_exceeds_max_retry_horizon`, `test_nats_streams.py::test_deduplication_same_event_id_stored_once` | LOCALLY_VERIFIED | passes against real container | No |
| S0-1B-003 | Step 1b | Durable, explicit-ack consumers w/ exponential backoff | `libs/mirage_common/nats_client.py::DeadLetterAwareConsumer` | `test_nats_streams.py::test_publish_and_consume`, `test_explicit_ack_...`, `test_redelivery_on_handler_failure` | LOCALLY_VERIFIED | passes against real container | No |
| S0-1B-004 | Step 1b | After 5 failed deliveries: publish to `*.failed`, ack original, include required fields, emit alert | `libs/mirage_common/nats_client.py::DeadLetterAwareConsumer._dead_letter`, `DeadLetterInfo` | `test_nats_streams.py::test_dead_letter_flow_and_stream_stays_unblocked` | LOCALLY_VERIFIED | dead-letter payload includes event_id, consumer_name, error_type, attempt_count, first/last failure timestamps; alert callback fires (asserted) | No |
| S0-1B-005 | Step 1b ✓Done | Poison message reaches dead-letter without blocking stream | `nats_client.py` | `test_nats_streams.py::test_dead_letter_flow_and_stream_stays_unblocked` | LOCALLY_VERIFIED | asserts a subsequent good message is still delivered normally after DLQ | No |
| S0-1B-006 | Step 1b ✓Done | Replayed message yields one effective state change | `nats_client.py` (pattern demonstrated; real enforcement lands with `processed_events` in Step 6) | `test_nats_streams.py::test_replay_yields_one_effective_state_change` | LOCALLY_VERIFIED | idempotent handler + fresh DeliverPolicy=ALL consumer proven to produce exactly one state mutation | No |
| S0-1B-007 | Step 1b (bonus) | Consumer restart resumes without reprocessing already-acked messages | `nats_client.py` (durable-name-based resumption, a JetStream property exercised, not app code) | `test_nats_streams.py::test_consumer_restart_resumes_without_reprocessing` | LOCALLY_VERIFIED | passes | No |

## Stage 0 / Step 2 — AWS foundation (Terraform)

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S0-2-001 | §5, Step 2 | Five-subnet VPC (public edge, control, endpoint, sandbox, attacker), private by default | `infra/terraform/modules/vpc` | `terraform validate` (both envs) | LOCALLY_VERIFIED | TEST_RESULTS.md §Step2 | Apply=Yes |
| S0-2-002 | §5 | No public IP on private-subnet instances | `infra/terraform/modules/vpc` | `tests/unit/test_terraform_network_policy.py::test_only_public_edge_subnet_has_public_ip` | LOCALLY_VERIFIED | passes; mutation-tested (injected violation caught, then reverted) | Apply=Yes |
| S0-2-003 | §5 | Attacker subnet reaches broker entry only; cannot reach control/evidence | `infra/terraform/modules/vpc` (security groups) | `test_terraform_network_policy.py::test_attacker_*`, `test_protected_security_groups_never_reference_attacker` | LOCALLY_VERIFIED | 5 dedicated tests pass; mutation-tested | Apply=Yes |
| S0-2-004 | §5 | S3/KMS/Secrets via VPC endpoints, no NAT gateway | `infra/terraform/modules/vpc` | `terraform validate`, `test_terraform_network_policy.py::test_vpc_endpoints_*`, `test_no_nat_gateway_anywhere` | LOCALLY_VERIFIED | passes | Apply=Yes |
| S0-2-005 | Step 2 | Evidence S3 bucket: versioning + Object Lock + encryption + access logging | `infra/terraform/modules/evidence` | `terraform validate`, tfsec | LOCALLY_VERIFIED | tfsec: 0 unresolved findings | Apply=Yes |
| S0-2-006 | Step 2 | IAM roles least-privilege per unit | `infra/terraform/modules/iam` | tfsec | LOCALLY_VERIFIED | 5 per-service policies + 1 control-node role (ADR-0011); tfsec clean | Apply=Yes |
| S0-2-007 | Step 2 | KMS asymmetric signing key (RSASSA_PSS_SHA_256) | `infra/terraform/modules/evidence` (`aws_kms_key.signing`, RSA_4096/SIGN_VERIFY) | `terraform validate` | LOCALLY_VERIFIED | passes | Apply=Yes |
| S0-2-008 | Prompt-1 instructions | `Project=mirage`, `Environment=<env>` tags on all resources | all modules (`default_tags` provider block + `local.common_tags`) | `terraform validate`, `test_local_common_tags_includes_project_and_environment` | LOCALLY_VERIFIED | passes | Apply=Yes |
| S0-2-009 | Prompt-1 instructions | `scripts/inventory-aws`, `scripts/verify-teardown` | `scripts/inventory-aws`, `scripts/verify-teardown` | code review (cannot execute without AWS creds) | CODE_COMPLETE | real `aws resourcegroupstaggingapi` calls; not run against a live account | Apply=Yes |
| S0-2-010 | Step 2 ✓Done | `terraform apply` from empty; reachability tests pass; `terraform destroy` leaves zero `Project=mirage` resources | | n/a locally | LAB_VERIFICATION_REQUIRED | see LAB_EXECUTION_CHECKLIST.md | Yes — full AWS |
| S0-2-011 | Step 2 local acceptance | Static security scan (tfsec) passes | all modules | `tfsec infra/terraform` | LOCALLY_VERIFIED | 288 passed, 89 ignored (all documented with justification comments), 0 unresolved critical/high/medium/low | No |
| S0-2-012 | Step 2 local acceptance | `terraform fmt -check` passes | all modules | `terraform fmt -check -recursive` | LOCALLY_VERIFIED | passes | No |
| S0-2-013 | Prompt-1 instructions | VPC Flow Logs enabled (defense-in-depth beyond spec's literal text, added to satisfy tfsec + genuine audit value) | `infra/terraform/modules/vpc` (`aws_flow_log.this`) | `terraform validate` | LOCALLY_VERIFIED | passes | Apply=Yes |

## Stage 0 / Step 3 — Trust, enrolment, rotation

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S0-3-001 | Step 3 | step-ca config w/ separate profiles: MirageEndpoint, MirageSpider, MirageEnvironmentController, broker clients, internal control | `infra/step-ca/PROFILES.md`, `libs/mirage_common/step_ca_admin.py`, `scripts/bootstrap-step-ca-provisioners` | `tests/integration/test_step_ca_enrollment.py` (via `step_ca_container` fixture) | LOCALLY_VERIFIED | 5 provisioners created + verified against real container; empirically-derived durations (24h agent / 168h broker+internal) | No |
| S0-3-002 | Step 3 | One-time enrolment token flow (10-step sequence in prompt) | `services/mirage-agent-ingestion/mirage_agent_ingestion/enrollment.py`, `libs/mirage_common/step_ca_client.py` | `tests/integration/test_step_ca_enrollment.py::test_full_enrollment_flow_issues_certificate` | LOCALLY_VERIFIED | passes against real Postgres + real step-ca; full JWT claim shape and /1.0/sign contract verified empirically (TEST_RESULTS.md §Step3) | No |
| S0-3-003 | Step 3 | Token expiry + reuse rejection (transactional invalidation) | `enrollment.py::enroll_agent` (single `UPDATE...WHERE used_at IS NULL AND expires_at > now()`) | `test_token_reuse_fails`, `test_expired_token_rejected` | LOCALLY_VERIFIED | both pass | No |
| S0-3-004 | Step 3 | Certificate renewal before 20% lifetime remains, preserves identity | `enrollment.py::renew_agent`, `renewal_due` | `test_renewal_preserves_identity` (integration), `test_renewal_due_*` (unit) | LOCALLY_VERIFIED | passes | No |
| S0-3-005 | Step 3 | Certificate revocation; revoked client rejected | `enrollment.py::revoke_agent`, `is_agent_active`; `step_ca_client.revoke_certificate` (passive, ADR-0013) | `test_revoked_certificate_is_refused`, `test_destroyed_sandbox_identity_cannot_reconnect` | LOCALLY_VERIFIED | passes; passive-vs-active revocation empirically verified (501 without `passive:true`, 200 with) | No |
| S0-3-006 | Step 3 | Build-hash allowlist + host-fingerprint validation | `enrollment.py::enroll_agent` (allowlist query + format checks), `infra/migrations/0001...` (`build_hash_allowlist` table) | `test_invalid_build_hash_fails` | LOCALLY_VERIFIED | passes | No |
| S0-3-007 | Step 3 | Audit events: enrolment, renewal, failure, revocation | `infra/migrations/0001...` (`certificate_history`), `schemas/events/agent.enrolled.v1` + 3 siblings (Step 1) | `test_certificate_history_records_full_lifecycle` | LOCALLY_VERIFIED | ISSUED/RENEWED/REVOKED rows verified in order; NATS event publication not yet wired (KNOWN_ISSUES.md — lands with Step 4b's HTTP layer / Step 6 outbox) | No |
| S0-3-008 | Step 3 ✓Done | Agent enrols over mTLS; token reuse fails; renewal preserves identity; destroyed sandbox cert refused | | `tests/integration/test_step_ca_enrollment.py` (8 tests) | LOCALLY_VERIFIED | 8/8 pass against real Postgres + real step-ca containers | No |
| S0-3-009 | Prompt-1 instructions | Test certificates for development only; no private CA keys committed | `infra/step-ca/dev-provisioner-keys/` (gitignored), `.gitignore` | `scripts/validate-config --scan-secrets` | LOCALLY_VERIFIED | scan clean; dir confirmed gitignored | No |

## Stage 1 / Step 4 — Employee endpoint (dev MSI)

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S1-4-001 | Step 4 | MirageEndpoint enrols with control plane | `agents/mirage-endpoint/mirage_endpoint/service_logic.py::enroll` | `tests/integration/test_mirage_endpoint_e2e.py::test_endpoint_enrolls_against_a_real_live_server` | LOCALLY_VERIFIED | real uvicorn+TLS server, real Postgres, real step-ca, real CSR/cert issuance | No |
| S1-4-002 | Step 4 | Persist certificate securely | `service_logic.py` (identity.json + cert files), `keys.py` (`DpapiKeyProvider` for the queue key) | same E2E test (files written + reloadable) | LOCALLY_VERIFIED / LAB for DPAPI path | cert persistence tested; DPAPI-specific protection is Windows-only | Yes — DPAPI path only |
| S1-4-003 | Step 4 | Maintain monotonic event sequence | `queue.py::next_sequence/current_sequence` | `tests/unit/test_endpoint_queue.py::test_sequence_persists_and_never_repeats_across_restart` | LOCALLY_VERIFIED | passes | No |
| S1-4-004 | Step 4 | Emit heartbeat / health state | `service_logic.py::build_heartbeat_payload`, `send_heartbeat` | `test_mirage_endpoint_e2e.py`, `test_agent_ingestion_api.py` (heartbeat endpoint) | LOCALLY_VERIFIED | passes | No |
| S1-4-005 | Step 4 | Maintain encrypted local queue | `queue.py::EncryptedEventQueue` (Fernet, key via `KeyProvider`) | `test_endpoint_queue.py::test_data_at_rest_is_encrypted_not_plaintext` | LOCALLY_VERIFIED | ciphertext confirmed absent of plaintext marker on disk | No |
| S1-4-006 | Step 4 | Retry after network outage | `service_logic.py::flush_queue` (order-preserving, partial-failure-safe) | `test_endpoint_queue.py::test_outage_replay_*` (2 tests) | LOCALLY_VERIFIED | passes | No |
| S1-4-007 | Step 4 | Report build hash and version | `service_logic.py` (`AGENT_VERSION`, `self.build_hash` in heartbeat payload); `__init__.py::__build_hash__` stamped by `build.ps1` | covered by heartbeat payload tests | LOCALLY_VERIFIED (stamping mechanism is lab-only) | payload includes both fields | Partial — stamping needs Windows build |
| S1-4-008 | Step 4 | Integrate with Sysmon and Elastic Agent | `installers/endpoint/config/sysmon-config.xml`, `Bundle.wxs` (chains Sysmon + Elastic Agent installers) | XML well-formedness only | CODE_COMPLETE | real Sysmon config authored; chain install untestable without Windows | Yes |
| S1-4-009 | Step 4 | Never execute arbitrary server commands | (structural: MirageEndpoint has no command-execution surface at all — only enroll/heartbeat/queue) | N/A — absence of a feature | LOCALLY_VERIFIED | reviewed: no code path accepts/executes server-supplied commands | No |
| S1-4-010 | Step 4 | Dev WiX MSI: service, Sysmon, config, dirs, firewall, registry, uninstaller | `installers/endpoint/Product.wxs` | XML well-formedness (`xml.etree` parse) | CODE_COMPLETE | parses as valid XML; WiX-schema compilation is lab-only (ADR-0008) | Yes |
| S1-4-011 | Step 4 | Exact paths (Program Files, ProgramData, Logs, Queue, cert store, registry, service name, event-log provider) | `agents/mirage-endpoint/mirage_endpoint/config.py`, mirrored in `Product.wxs` | reviewed for 1:1 correspondence | CODE_COMPLETE | | No |
| S1-4-012 | Step 4 | Secure ACLs | `Product.wxs` (`util:PermissionEx`, LOCAL SERVICE-scoped) | reviewed | CODE_COMPLETE | actual ACL enforcement is lab-only | Yes |
| S1-4-013 | Step 4 | PowerShell install/start/stop/upgrade/rollback/uninstall/queue-recovery/cert-renewal/firewall scripts | `installers/endpoint/scripts/*.ps1` (3 scripts covering all named scenarios) | none locally possible | CODE_COMPLETE | real PowerShell, structurally reviewed | Yes — all |
| S1-4-014 | Step 4 | Elastic templates for endpoint telemetry | `infra/elastic/{component_templates,index_templates,ingest_pipelines,ilm}/*.json` | `tests/integration/test_elastic_templates.py` (4 tests) | LOCALLY_VERIFIED | real ES 8.15 container: index, search, strict-mapping rejection, ILM policy all verified | No |
| S1-4-015 | Step 4 local ✓Done | Service compiles; MSI source compiles where WiX available; config validation passes; queue sequence persists across restart; outage replay tests pass | | unit+integration suite (116 tests total incl. these) | LOCALLY_VERIFIED (MSI compile is lab) | all local pieces pass | Yes — MSI compile only |
| S1-4-016 | Step 4 lab ✓Done | Endpoint event reaches Elasticsearch p95<3s; buffers >=15min; 5-min outage zero loss | | `installers/endpoint/scripts/test-queue-recovery.ps1` | LAB_VERIFICATION_REQUIRED | not run — needs real Windows host + deployed control plane | Yes — full stack |

## Stage 1 / Step 4b — Early engineering console

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S1-4B-001 | Step 4b | Authenticate console access via Keycloak OIDC (JWKS-verified bearer tokens) | `libs/mirage_common/oidc.py::OidcVerifier` | `tests/integration/test_mirage_api.py::test_no_token_is_rejected` | LOCALLY_VERIFIED | real Keycloak container, real RS256 JWT verified via PyJWKClient | No |
| S1-4B-002 | Step 4b | Only platform_admin may access privileged engineering diagnostics | `services/mirage-api/mirage_api/app.py::require_platform_admin`, `mirage_common/oidc.py::require_any_role` | `test_platform_admin_can_view_health`, `test_unauthorized_role_is_refused` | LOCALLY_VERIFIED | real `dev-platform-admin` token → 200; real `dev-read-only` token → 403 | No |
| S1-4B-003 | Step 4b | Console reports live health of every platform component | `services/mirage-api/mirage_api/health.py::run_all_checks` (real Postgres SELECT 1, real NATS `/healthz`, real ES `/_cluster/health`, real Keycloak discovery doc, real step-ca `/health`, real agent-ingestion `/health`; SANDBOX_GATEWAY/OUTBOX_RELAY/WORKER honestly reported UNKNOWN — not built until Steps 6/8) | `test_platform_admin_can_view_health` | LOCALLY_VERIFIED | 9 components reported; 3 not-yet-built ones return UNKNOWN, never a fabricated HEALTHY | No |
| S1-4B-004 | Step 4b | Dependency failure changes health status | `health.py::overall_status` (UNHEALTHY/DEGRADED propagate; UNKNOWN does not) | `test_dependency_failure_changes_health_status` | LOCALLY_VERIFIED | Postgres pointed at `127.0.0.1:1` (real refused connection) → component UNHEALTHY, overall UNHEALTHY | No |
| S1-4B-005 | Step 4b | Console can list enrolled agents and open cases from real Postgres | `app.py::list_agents`, `list_cases`; `infra/migrations/0002_cases_minimal.up.sql` | `test_list_agents_queries_real_postgres`, `test_list_cases_queries_real_postgres` | LOCALLY_VERIFIED | rows inserted directly via psycopg, read back through the real HTTP endpoint | No |
| S1-4B-006 | Step 4b | Console can show recent telemetry events | `app.py::recent_events` (queries `mirage-telemetry-endpoint,mirage-health` data streams, sorted by `ingest_time desc`) | reviewed; exercised indirectly via `test_synthetic_health_check_is_traceable_end_to_end` (event becomes searchable) | LOCALLY_VERIFIED | honest empty-list on 404 (no data stream yet) rather than a fabricated error or fake data | No |
| S1-4B-007 | Step 4b | Synthetic event is traceable end to end (generated → published via NATS → indexed in Elasticsearch → confirmed searchable), one correlation ID throughout | `services/mirage-api/mirage_api/synthetic.py::run_synthetic_health_check` | `test_synthetic_health_check_is_traceable_end_to_end` | LOCALLY_VERIFIED | real NATS JetStream publish + `DeadLetterAwareConsumer` delivery confirmation + real ES index + real ES search hit, single ULID correlation_id across all three | No |
| S1-4B-008 | Step 4b | New `system.health` events are ingestible (index template, mapping, timestamp pipeline) | `infra/elastic/index_templates/mirage-health.json` (composed of `mirage-common-mappings`, `mirage-set-timestamp` pipeline) | `test_synthetic_health_check_is_traceable_end_to_end` (indexes + searches for real) | LOCALLY_VERIFIED | real ES 8.15 container accepts the template and indexes documents | No |
| S1-4B-009 | Step 4b | CLI operator tool for health checks (scriptable, CI-usable) | `scripts/mirage-health` | manually exercised against dev stack; exit code contract reviewed (0 only on HEALTHY) | CODE_COMPLETE | no dedicated automated test — thin argument-parsing/HTTP-call wrapper around already-tested `/api/v1/health` | No |
| S1-4B-010 | Step 4b | Keycloak realm/roles/client/dev users provisioned idempotently, reusable by tests and real dev stack | `libs/mirage_common/keycloak_admin.py::bootstrap_realm`, `scripts/bootstrap-keycloak-realm` | `tests/integration/conftest.py::keycloak_realm` fixture (calls the same function); re-run manually against persistent dev container to confirm idempotency | LOCALLY_VERIFIED | ran twice against dev Keycloak — second run made zero changes, no errors | No |
| S1-4B-011 | Step 4b | Engineering dashboard visible in Kibana (not the Stage 9 analyst product — a dev/ops view) | `infra/kibana/mirage-engineering-dashboard.ndjson` | manual import via real Kibana 8.15.0 container's `POST /api/saved_objects/_import` | LOCALLY_VERIFIED | `successCount: 5`, all 5 objects (2 index-patterns, 1 search, 1 visualization, 1 dashboard) imported with references intact; dashboard object re-fetched and confirmed resolvable | No |
| S1-4B-012 | Step 4b ✓Done | Authenticated admin can view health; unauthorized role is refused; dependency failure changes health status; synthetic event is traceable end to end | | `tests/integration/test_mirage_api.py` (7 tests) | LOCALLY_VERIFIED | 7/7 pass against real Postgres, NATS, Elasticsearch, Keycloak containers | No |

## Stage 1 / Step 5 — MirageSpider (sandbox sensing)

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S1-5-001 | Step 5 | MirageSpider is a read-only Windows service, LocalService account, never LocalSystem | `agents/mirage-spider/mirage_spider/config.py` (`SERVICE_ACCOUNT`), `win_service.py` (LocalService, ADR-0002 Windows-only guard); structurally, `SpiderServiceLogic` exposes no filesystem/process-mutation method at all | reviewed (no code path writes to the sandbox); win_service.py itself is LAB_VERIFICATION_REQUIRED (Windows-only import) | CODE_COMPLETE (business logic LOCALLY_VERIFIED, service shim LAB) | | Yes — service shim only |
| S1-5-002 | Step 5 | Spider enrols over mTLS with its own cert profile | `agents/mirage-spider/mirage_spider/service_logic.py::enroll` (role=SPIDER, reuses the `mirage-spider` step-ca profile provisioned in Step 3) | `tests/integration/test_mirage_spider_e2e.py::test_spider_enrolls_against_a_real_live_server_and_queues_ordered_case_tagged_events` | LOCALLY_VERIFIED | real uvicorn+TLS server, real Postgres, real step-ca, real CSR/cert issuance | No |
| S1-5-003 | Step 5 | Monotonic sequence per certificate identity; encrypted local queue | `libs/mirage_common/agent_queue.py::EncryptedEventQueue` (shared with MirageEndpoint, ADR-0015), `service_logic.py::record_observation` (assigns via `queue.next_sequence()`) | `tests/unit/test_agent_queue.py`, `tests/unit/test_spider_service_logic.py::test_record_observation_assigns_increasing_case_tagged_sequence` | LOCALLY_VERIFIED | passes | No |
| S1-5-004 | Step 5 | Events arrive ordered and case-tagged | `service_logic.py::record_observation` (case_id/session_id passed through to the shared envelope), `mirage_agent_ingestion/app.py::submit_telemetry` (server-side monotonic-sequence enforcement, 409 on replay/out-of-order) | `test_agent_ingestion_api.py::test_telemetry_endpoint_accepts_and_publishes_a_spider_observation`, `::test_telemetry_endpoint_rejects_out_of_order_sequence`; `test_mirage_spider_e2e.py::test_spider_enrolls_...` (queue-level ordering) | LOCALLY_VERIFIED | real NATS JetStream publish confirmed via durable consumer; real 409 on replay against real Postgres row-locked sequence check | No |
| S1-5-005 | Step 5 | Sequence survives a 5-min outage with zero loss | `service_logic.py::flush_queue` (order-preserving, partial-failure-safe, async transport), local queue persists across process restart | `test_mirage_spider_e2e.py::test_spider_sequence_survives_restart_with_zero_loss` (restart simulated as fresh objects over the same on-disk queue.db) | LOCALLY_VERIFIED | 3 events survive a simulated restart with correct continued sequence [1,2,3], zero loss | No |
| S1-5-006 | Step 5 | Low-noise, not invisible (routine telemetry) | `service_logic.py::record_observation` + `flush_queue` (routine, batched, no special priority) | covered by S1-5-004/005 tests | LOCALLY_VERIFIED | | No |
| S1-5-007 | Step 5 | Tamper + health high-priority | `service_logic.py::record_tamper` (immediate-send-else-durable-queue-fallback, never touches the routine queue on success); health reuses Step 4's heartbeat path unchanged | `tests/unit/test_spider_service_logic.py::test_tamper_event_sends_immediately_and_never_touches_queue_on_success`, `::test_tamper_event_falls_back_to_durable_queue_on_send_failure`; `test_agent_ingestion_api.py::test_telemetry_tamper_event_routes_to_audit_stream`; `test_mirage_spider_e2e.py::test_spider_tamper_event_falls_back_to_the_durable_queue_on_a_real_send_failure` (real, unscripted rejection) | LOCALLY_VERIFIED | fake-transport success/failure paths + a real network failure all proven; tamper events routed to the immutable `audit.spider.tamper` subject (ADR-0015) | No |
| S1-5-008 | Step 5 | Real telemetry ingestion endpoint (closes the gap Step 4 deferred) | `services/mirage-agent-ingestion/mirage_agent_ingestion/app.py::submit_telemetry` (`POST /api/v1/agents/{id}/telemetry`) — identity/status checks, event_type allowlist, source_id spoofing check, server-stamped ingest_time, real `validate_event()`, real NATS publish | `test_agent_ingestion_api.py` (4 new tests: accepts+publishes, rejects out-of-order, rejects disallowed event_type, tamper routes to audit) | LOCALLY_VERIFIED | 4/4 pass against real Postgres + real NATS JetStream | No |
| S1-5-009 | Step 5 | Elastic data stream for sandbox telemetry (Appendix E: "Spider telemetry, case-tagged") | `infra/elastic/index_templates/mirage-telemetry-sandbox.json` | manually indexed + searched a real `spider.observation` event against the real dev Elasticsearch container: `201 Created`, searchable, `@timestamp` correctly derived | LOCALLY_VERIFIED | real ES 8.15, real dev stack (not just a test container) | No |
| S1-5-010 | Step 5 ✓Done | Spider events arrive ordered and case-tagged; sequence survives a 5-min outage with zero loss; runs read-only, not LocalSystem | | `tests/unit/test_spider_service_logic.py` (4 tests) + `tests/integration/test_mirage_spider_e2e.py` (3 tests) + `tests/integration/test_agent_ingestion_api.py` (4 new telemetry tests) | LOCALLY_VERIFIED | 11/11 new tests pass; full regression suite (136 tests total) green | No |

## Stage 2 / Step 6 — Case state machine + migrations + outbox

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S2-6-001 | Step 6 | Tables + migrations before services start; rollback tested | `infra/migrations/0003_case_lifecycle_and_outbox.{up,down}.sql` (case_state_transitions, outbox_events + NOTIFY trigger, processed_events, audit_events; extends migration 0002's `cases` rather than replacing it) | manual `scripts/migrate up` / `down` / `up` against the real dev Postgres container | LOCALLY_VERIFIED | applied, rolled back, re-applied cleanly against real Postgres 16 | No |
| S2-6-002 | Step 6 | Ten-state case lifecycle: CREATED→ARMED→MONITORING→STEERING_PENDING→SANDBOX_ACTIVE→ENGAGING→CONCLUDING→EVIDENCE_VERIFYING→EXPORTED→DESTROYED | `libs/mirage_common/case_state_machine.py::ALLOWED_TRANSITIONS` | `tests/unit/test_case_state_machine.py` (graph shape), `tests/integration/test_case_state_machine_e2e.py::test_case_runs_every_state_in_order_with_audit_and_outbox_rows` | LOCALLY_VERIFIED | a real case driven through all 10 states against real Postgres; also manually verified against the persistent dev DB | No |
| S2-6-003 | Step 6 | Optimistic locking (WHERE version = ?) | `case_state_machine.py::transition_case` (`SELECT ... FOR UPDATE` + version check + conditional `UPDATE ... WHERE version = %s`) | `test_case_state_machine_e2e.py::test_replaying_a_transition_with_a_stale_version_is_rejected_not_double_applied` | LOCALLY_VERIFIED | stale-version replay rejected with `OptimisticLockConflictError`; case state/version and row counts confirmed unchanged by the rejected attempt | No |
| S2-6-004 | Step 6 | Every transition writes an audit + outbox event in one transaction | `case_state_machine.py::transition_case` (single Postgres transaction: `cases` UPDATE, `case_state_transitions` INSERT, `audit_events` INSERT, 2x `outbox_events` INSERT — `case.state_changed` + `audit.recorded`) | `test_case_runs_every_state_in_order_with_audit_and_outbox_rows` (18 outbox rows / 9 audit rows for 9 transitions) | LOCALLY_VERIFIED | row counts verified exactly against real Postgres | No |
| S2-6-005 | Step 6 | mirage-outbox-relay per §6.3 (SELECT ... FOR UPDATE SKIP LOCKED, publish-before-mark, alert after 10 attempts) | `services/mirage-outbox-relay/mirage_outbox_relay/relay.py::OutboxRelay` | `test_case_state_machine_e2e.py::test_outbox_relay_publishes_case_state_changed_to_real_nats`, `::test_outbox_relay_retries_and_alerts_after_repeated_failure` | LOCALLY_VERIFIED | real NATS JetStream publish + durable-consumer confirmation; real repeated-failure path (unroutable subject) confirmed to alert after exactly `ALERT_AFTER_ATTEMPTS`; also manually run against the persistent dev stack (`scripts/run-outbox-relay`, one real batch: 2/2 published) | No |
| S2-6-006 | Step 6 | Replays produce zero duplicate effective events | NATS `Nats-Msg-Id` dedup (`publish_event`, Step 1b) + `processed_events` idempotency table (schema only in Prompt 1 — no consumer writes it yet, since no Step-6-scope consumer exists; Step 7's detection adapter is the first real writer) | covered structurally by S2-6-003 (state mutation itself can't double-apply) and S2-6-005 (publish-before-mark + JetStream dedup, already proven for other event types in Step 1b) | LOCALLY_VERIFIED (state mutation) / CODE_COMPLETE (processed_events table, unused until Step 7) | | No |
| S2-6-007 | Step 6 ✓Done | A case runs every state and replays with zero conflicting-state bugs and zero duplicate effective events | | `tests/unit/test_case_state_machine.py` (4 tests) + `tests/integration/test_case_state_machine_e2e.py` (5 tests) | LOCALLY_VERIFIED | 9/9 new tests pass; full regression suite (145 tests total) green | No |

## Stage 2 / Step 7 — Detection into cases

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S2-7-001 | Step 7 | Consume detections (Elastic Defend / Suricata adapter events) | `services/mirage-worker/mirage_worker/detection_adapter.py::DetectionAdapter` (durable, dead-letter-aware NATS consumer bound to `investigation.detection.raised`, reusing Step 1b's `DeadLetterAwareConsumer`) | `tests/integration/test_detection_correlation.py::test_detection_adapter_consumer_loop_processes_real_nats_events` | LOCALLY_VERIFIED | real NATS JetStream publish + real consumer processing, 2/2 processed | No |
| S2-7-002 | Step 7 | Dedup | `libs/mirage_common/detection_correlation.py::correlate_detection` (`processed_events(consumer_name, event_id)` lookup — the first real writer to that Step-6-built table) | `test_redelivered_detection_is_deduped_not_double_processed` | LOCALLY_VERIFIED | a redelivered detection_event_id produces zero additional case/audit rows | No |
| S2-7-003 | Step 7 | Correlate into one case; assign severity + confidence | `detection_correlation.py::correlate_detection` (`cases.correlation_key` lookup — migration 0004; new case on miss, existing case + audit row on hit) | `test_first_detection_creates_exactly_one_case_with_immutable_first_event`, `test_second_detection_with_same_correlation_key_attaches_to_existing_case` | LOCALLY_VERIFIED | two detections sharing a correlation_key produce exactly one case; severity/confidence carried into the case row / audit detail | No |
| S2-7-004 | Step 7 | Publish investigation.created via outbox | `correlate_detection` (writes `case.created` to `outbox_events`, topic=`investigation.case.created`, inside the same transaction as the `cases` INSERT) | `test_first_detection_creates_exactly_one_case_with_immutable_first_event` (exactly 1 outbox row, correct payload) | LOCALLY_VERIFIED | verified against real Postgres; also manually verified against the persistent dev DB (2 correlated detections -> 1 case, 1 outbox row) | No |
| S2-7-005 | Step 7 | Adapter never steers; case creation + steering are operator-approved | `detection_correlation.py` has no call to `case_state_machine.transition_case` anywhere (ADR-0017) — a created case is inserted directly at `state='CREATED'` and never advanced | `test_adapter_never_advances_case_state_past_created` | LOCALLY_VERIFIED | case state/version and `case_state_transitions` row count confirmed untouched after correlation | No |
| S2-7-006 | Step 7 ✓Done | An alert yields exactly one correlated case with an immutable first lifecycle event | | `tests/integration/test_detection_correlation.py` (5 tests) | LOCALLY_VERIFIED | 5/5 pass against real Postgres + real NATS; full regression suite (150 tests total) green | No |

## Stage 2 / Step 7b — Development sandbox target

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S2-7B-001 | Step 7b | Dev sandbox accepts HTTP | `infra/compose/docker-compose.dev-sandbox.yml::dev-sandbox-http` (nginx) | `tests/integration/test_dev_sandbox_target.py::test_dev_sandbox_accepts_real_http_connections` | LOCALLY_VERIFIED | real HTTP 200; also manually verified against the persistent compose stack | No |
| S2-7B-002 | Step 7b | Dev sandbox accepts SSH | `docker-compose.dev-sandbox.yml::dev-sandbox-ssh` (linuxserver/openssh-server, key-based auth) | `test_dev_sandbox_accepts_real_ssh_connections` | LOCALLY_VERIFIED | real SSH connection + command execution succeeds; also manually verified against the persistent compose stack | No |
| S2-7B-003 | Step 7b | Dev sandbox accepts RDP | none — no viable local RDP server (ADR-0018) | none locally possible | LAB_VERIFICATION_REQUIRED | | Yes — real Windows sandbox + RD Gateway required |
| S2-7B-004 | Step 7b | Dev sandbox reports Spider telemetry | `tests/integration/test_dev_sandbox_target.py::test_spider_reports_telemetry_observed_from_the_dev_sandbox` (Step 5's `SpiderServiceLogic`, real enroll + real observation events about the real HTTP/SSH services running on the target) | same test | LOCALLY_VERIFIED | 3 real observation events queued, real heartbeat payload reflects them | No |
| S2-7B-005 | Step 7b | Fingerprintable profile (§6.5: "a versioned file with named comparators") | `infra/fingerprint/baseline.schema.json`, `dev-sandbox-baseline.v1.json` (all 8 §6.5 checks, correct MUST/SHOULD levels) | `tests/unit/test_fingerprint_baseline.py` (4 tests) | LOCALLY_VERIFIED | baseline validates against its schema; all 8 checks present with correct levels; forbidden_patterns includes Mirage*/Spider* | No |
| S2-7B-006 | Step 7b ✓Done | The dev sandbox accepts the three protocols and reports Spider telemetry — a valid steering target exists | | `tests/integration/test_dev_sandbox_target.py` (3 tests) + `tests/unit/test_fingerprint_baseline.py` (4 tests) | LOCALLY_VERIFIED (HTTP/SSH/telemetry) / LAB_VERIFICATION_REQUIRED (RDP) | 7/7 new local tests pass; full regression suite (157 tests total) green | Yes — RDP leg only |

## Stage 3 / Step 8a — The /route decision API

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S3-8A-001 | Step 8a | `routing_decisions` table with overlap-uniqueness constraint | `infra/migrations/0005_routing_decisions.up.sql` (real Postgres `EXCLUDE USING gist` over `(match_key, tstzrange(valid_from, valid_until))`, `btree_gist` extension) | `tests/integration/test_routing_api.py::test_steer_rejects_overlapping_active_decision_for_same_match_key` | LOCALLY_VERIFIED | real `psycopg.errors.ExclusionViolation` on conflicting write, mapped to HTTP 409; migration up/down/up verified against real dev Postgres | No |
| S3-8A-002 | Step 8a | `/route` returns SANDBOX for a matching active decision, ENDPOINT (default) otherwise | `libs/mirage_common/routing.py::resolve_route`, `services/mirage-api/mirage_api/app.py::route` | `test_route_defaults_to_endpoint_when_no_decision_exists`, `test_steer_then_route_returns_sandbox_for_matching_key` | LOCALLY_VERIFIED | real Postgres lookups against real decisions | No |
| S3-8A-003 | Step 8a | 1-second in-memory TTL cache (Postgres authoritative) | `routing.py::RouteCache` | `test_route_cache_serves_second_call_from_cache` | LOCALLY_VERIFIED | second call within the TTL window returns `cached: True` with the same upstream | No |
| S3-8A-004 | Step 8a | Decision write side ("Analyst approves steering -> mirage-api writes routing_decisions") — `POST /api/v1/cases/{id}/steer` | `routing.py::write_routing_decision`, `app.py::steer_case` (platform_admin-gated) | `test_steer_then_route_returns_sandbox_for_matching_key`, `test_steer_requires_platform_admin` | LOCALLY_VERIFIED | real Keycloak-token-gated write; real 403 for `dev-read-only` | No |
| S3-8A-005 | Step 8a | Steering event logged / recorded on the case timeline | `routing.py` publishes `steering.decision_recorded` (action=CREATED on write, SELECTED/FAILSAFE_DEFAULT on every `/route` resolution) via the outbox, plus an `audit_events` row on write | `test_steering_decision_recorded_events_are_published_via_outbox` | LOCALLY_VERIFIED | real outbox rows with correct `action` sequence confirmed against real Postgres | No |
| S3-8A-006 | Step 8a | `/route` is caller-authenticated (brokers, not human operators) | `mirage_common/mtls_auth.py` (moved from mirage-agent-ingestion, ADR-0019) + an active-`BROKER_CLIENT`/`INTERNAL_CONTROL`-agent check | `test_route_requires_mtls_headers`, `test_route_rejects_non_broker_role` | LOCALLY_VERIFIED | real 401 (no headers), real 403 (wrong role) against a real enrolled agent | No |
| S3-8A-007 | Step 8a ✓Done | Given an approved decision, /route returns the sandbox for the matching key and the endpoint otherwise, logging the steering event | | `tests/integration/test_routing_api.py` (9 tests) | LOCALLY_VERIFIED | 9/9 pass against real Postgres, real NATS, real Keycloak, a real enrolled BROKER_CLIENT agent | No |

## Stage 3 / Step 8b/8c/8d — The three brokers

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S3-8BCD-001 | Step 8b | HTTP: Nginx calls /route (auth_request) before upstream selection; TLS/headers/WebSocket consistent | `infra/broker/http/nginx.conf.template` | `tests/integration/test_http_broker.py` (2 tests) | LOCALLY_VERIFIED | real Nginx + real mirage-api + real distinguishable backends; default-to-ENDPOINT and steer-to-SANDBOX both proven with real HTTP responses | No |
| S3-8BCD-002 | Step 8b | Backend chosen once, before establishment; no mid-session migration claim | Each HTTP request independently re-resolves `/route` (no persistent-connection-switch code path exists) | covered by S3-8BCD-001's tests (fresh request per assertion) | LOCALLY_VERIFIED | | No |
| S3-8BCD-003 | Step 8c | SSH: bastion ForceCommand calls /route before opening the backend channel | `infra/broker/ssh/mirage-route-selector.sh` | `tests/integration/test_ssh_broker.py` (2 tests) | LOCALLY_VERIFIED | real OpenSSH bastion, a real nested ssh session landing in the correct real backend container, confirmed via `cat` of a per-container marker file | No |
| S3-8BCD-004 | Step 8c | No claim that an established SSH connection is moved | Bastion `exec`s a brand-new ssh session per ForceCommand invocation (once per new client connection), never reparents an existing channel | covered by S3-8BCD-003's tests (each test is a fresh connection) | LOCALLY_VERIFIED | | No |
| S3-8BCD-005 | Step 8d | RDP: gateway policy calls /route before establishing the desktop | `infra/broker/rdp/README.md` (design document — RD Gateway's dynamic selection point is a compiled COM plugin, `IRDGPolicyEngine`, not a config file; see ADR-0020 for why no code is fabricated here) | none possible without a Windows host | LAB_VERIFICATION_REQUIRED | | Yes — Windows Server + RD Gateway role required |
| S3-8BCD-006 | Step 8b/8c/8d ✓Done (HTTP/SSH legs) | Each protocol routes to the sandbox per decision and passes its own continuity + fingerprint suite; no mid-session migration claim exists | | `tests/integration/test_http_broker.py` + `test_ssh_broker.py` (4 tests) | LOCALLY_VERIFIED (HTTP/SSH) / LAB_VERIFICATION_REQUIRED (RDP) | 4/4 pass against real Nginx/OpenSSH + real mirage-api; full regression suite (170 tests total) green. Fingerprint SUITE (§6.5, full checklist) is Step 10's own scope, not re-tested here — this step proves ROUTING, not the fingerprint gate | Yes — RDP leg only |

## Stage 4 / Step 9a — Golden image (Packer)

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S4-9A-001 | Step 9a | §6.5 fingerprint comparator engine: 8 named checks, 6 comparators, MUST=100%/SHOULD>=75% scoring | `libs/mirage_common/fingerprint.py` | `tests/unit/test_fingerprint_engine.py` (8 tests) | LOCALLY_VERIFIED | 8/8 pass, using the real Step 7b baseline file; covers exact-pass, missing-observation, forbidden-process hard-fail, sanctioned-exception carve-out, file-predates-hire-date, missing-required-software, SHOULD-threshold boundary, SHOULD-vs-MUST independence | No |
| S4-9A-002 | Step 9a | Packer template: source AMI -> install -> config -> employee profile -> fingerprint harness -> malware scan -> SBOM -> capture, in that exact order | `infra/packer/employee-sandbox.pkr.hcl`, `infra/packer/variables.pkr.hcl` | `tests/unit/test_packer_pipeline.py` (16 tests) | LOCALLY_VERIFIED (structure) / LAB_VERIFICATION_REQUIRED (live build) | 16/16 static-HCL tests pass — no public IP, WinRM-over-TLS, required tags, exact provisioner order, restart-before-harness, harness-before-scan-before-sbom, no hardcoded secrets | Yes — real AWS account + Windows AMI base |
| S4-9A-003 | Step 9a | Fingerprint harness runs at build time and aborts the build on any MUST-check failure | `infra/packer/scripts/run-fingerprint-harness.ps1` (embeds `libs/mirage_common/fingerprint.py`) | reviewed; exercised indirectly via S4-9A-001's engine tests | CODE_COMPLETE | exit-1-on-failure logic reviewed; cannot execute against a real Windows guest here | Yes — Windows host |
| S4-9A-004 | Step 9a | Malware scan (Windows Defender full scan) fails the build on any detection | `infra/packer/scripts/run-malware-scan.ps1` | reviewed | CODE_COMPLETE | `Update-MpSignature` + `Start-MpScan -ScanType FullScan` + throw-on-detection reviewed | Yes — Windows host |
| S4-9A-005 | Step 9a | SBOM generated for every golden image | `infra/packer/scripts/generate-sbom.ps1` | reviewed | CODE_COMPLETE | syft (CycloneDX) primary path + native registry-inventory fallback reviewed | Yes — Windows host |
| S4-9A-006 | Step 9a | AMI manifest KMS-signed; AMI tagged with manifest hash; Terraform consumes only approved AMI IDs | `scripts/sign-ami-manifest` | manual run with `--dry-run` (builds/prints manifest, no AWS calls) | CODE_COMPLETE | `--dry-run` path exercised without credentials; refuses to sign when the fingerprint report didn't pass; real `kms.sign`/`ec2.create_tags` calls are LAB-only (`boto3`, new `aws-lab` optional dependency group) | Yes — real AWS account + KMS key |
| S4-9A-007 | Step 9a | Employee/sandbox target uses the identical Sysmon config and MirageSpider agent already proven in Steps 4/5 (no drift between endpoint and golden image) | `infra/packer/scripts/install-sysmon.ps1` (reuses `installers/endpoint/config/sysmon-config.xml`), `infra/packer/scripts/install-mirage-spider.ps1` | reviewed | CODE_COMPLETE | scripts reference the same config file Step 4 already ships; service registration verified structurally via `Get-CimInstance Win32_Service` check in-script | Yes — Windows host |

## Stage 4 / Step 9b — Environment Controller + output tagging

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S4-9B-001 | Step 9b, Appendix B | `sandbox_instances`/`sandbox_actions` tables; `expected_state_version` optimistic concurrency | `infra/migrations/0006_sandbox_actions.{up,down}.sql` | integration tests below; up/down/up verified against real dev DB | LOCALLY_VERIFIED | migration cycle applied cleanly against the real persistent `mirage-postgres` container | No |
| S4-9B-002 | Step 9b, Appendix G | MirageEnvironmentController enrols via step-ca (role=ENV_CONTROLLER, reserved since Step 3) over a restricted, dedicated (never LocalSystem) account | `agents/mirage-env-controller/mirage_env_controller/{config,service_logic}.py` | `tests/integration/test_sandbox_gateway.py` (real step-ca enrolment) | LOCALLY_VERIFIED (enrolment) / LAB_VERIFICATION_REQUIRED (real restricted account + golden image) | real cert issued against a real ephemeral step-ca container, ENV_CONTROLLER role, existing provisioner | Yes — Windows host + golden image install script |
| S4-9B-003 | Step 9b, Appendix I | Accepts only the fixed action set; unknown action_type rejected | `libs/mirage_common/sandbox_actions.py` (`ALLOWED_ACTION_TYPES`), `mirage_sandbox_gateway/app.py` | `tests/unit/test_sandbox_actions_shared.py` (9 tests, schema/migration/constant consistency) | LOCALLY_VERIFIED | schema enum, migration CHECK constraint, and Python constant all asserted equal | No |
| S4-9B-004 | Step 9b | Structured action executes for real (filesystem mutation) and rolls back with full audit | `agents/mirage-env-controller/mirage_env_controller/actions.py` | `tests/unit/test_env_controller_actions.py` (21 tests); `tests/integration/test_sandbox_gateway.py::test_structured_action_executes_and_rolls_back_with_full_audit` | LOCALLY_VERIFIED | real PLACE_ARTIFACT + hash verification + rollback proven both in isolation and end-to-end (real Postgres audit rows, real WS round trip) | No |
| S4-9B-005 | Step 9b, Appendix G | "Privilege: Only approved mutation dirs/services" enforced in code (path-traversal-safe) | `mirage_env_controller.actions._resolve_within_roots` | `test_path_traversal_via_dotdot_is_rejected`, `test_restricted_path_is_rejected_end_to_end` | LOCALLY_VERIFIED | traversal attempt via `..` rejected both as a unit test and end-to-end through the live gateway | No |
| S4-9B-006 | Step 9b | Every output tagged REAL_OS_OUTPUT/DECOY_SERVICE_OUTPUT/AI_GENERATED_INTERACTION/ANALYST_MESSAGE | `mirage_common.sandbox_actions.default_output_tag` | `test_sandbox_actions_shared.py`, `test_env_controller_actions.py` (DISPLAY_MESSAGE / decoy-service cases) | LOCALLY_VERIFIED | every action type asserted to produce one of the 4 known tags; DISPLAY_MESSAGE's caller-supplied tag validated (rejects an invalid tag) | No |
| S4-9B-007 | Step 9b | Command delivered over outbound WSS to mirage-sandbox-gateway, optimistic-concurrency-checked, audited via the transactional outbox | `services/mirage-sandbox-gateway/mirage_sandbox_gateway/app.py` | `tests/integration/test_sandbox_gateway.py` (5 tests) | LOCALLY_VERIFIED | real live uvicorn server + real WS controller client; 409 on stale `expected_state_version`, 401 unauthenticated, 404 on a never-connected sandbox, full PLACE_ARTIFACT+ROLLBACK round trip with real `sandbox_actions`/`audit_events` rows | No |
| S4-9B-008 | Step 9b | Soft reset < 3 min, full rebuild < 10 min | `mirage_env_controller.actions._soft_reset`/`_full_rebuild` | `test_soft_reset_wipes_and_restores_within_spec_threshold`, `test_full_rebuild_wipes_deeper_including_session_marker` | LOCALLY_VERIFIED (mechanism + local timing) / LAB_VERIFICATION_REQUIRED (real AWS EC2 replace-instance timing) | real wipe-and-reseed proven, real `time.monotonic()` elapsed time recorded and asserted under threshold locally; the spec's threshold is ultimately about real AWS instance-replacement latency, which needs a real AWS account (ADR-0022 decision 7) | Yes — real AWS account |

## Stage 4 / Step 10 — Deception-quality gate (blocking)

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| S4-10-001 | Step 10 | Live fingerprint gate blocks SANDBOX_ACTIVE -> ENGAGING on any MUST failure | `libs/mirage_common/fingerprint_gate.py` | `tests/integration/test_fingerprint_gate_e2e.py` (6 tests) | LOCALLY_VERIFIED | real Postgres: a sabotaged MUST check (forbidden process) blocks the transition, case stays in SANDBOX_ACTIVE, real audit/outbox rows recorded | No |
| S4-10-002 | Step 10 | A passing evaluation (MUST=100%, SHOULD>=75%) advances the case for real | `enforce_fingerprint_gate_before_engaging` -> `transition_case` | `test_passing_snapshot_advances_sandbox_active_to_engaging` | LOCALLY_VERIFIED | real Postgres transition to ENGAGING, version bump, `fingerprint_gate.passed` audit row, `investigation.fingerprint_gate_evaluated` outbox row, all in one real transaction | No |
| S4-10-003 | Step 10, §6.5 | "An inconsistent sandbox is worse than none" — a sandbox with no reported observations at all is a hard failure, not a silent pass | `_evaluate_and_record` (observed defaults to `{}`, reusing `fingerprint.py`'s own missing-observation handling) | `test_no_snapshot_at_all_is_treated_as_a_hard_failure` | LOCALLY_VERIFIED | real Postgres, no snapshot row exists, gate blocks | No |
| S4-10-004 | Step 10 | Live observation channel: MirageSpider reports the §6.5 checklist for real, feeding the gate's read side | `schemas/events/spider.fingerprint_snapshot.v1.schema.json`, `SpiderServiceLogic.submit_fingerprint_snapshot`, `mirage_agent_ingestion`'s telemetry-endpoint upsert into `sandbox_fingerprint_snapshots` | `tests/unit/test_spider_service_logic.py` (2 new tests), `tests/integration/test_agent_ingestion_api.py::test_telemetry_fingerprint_snapshot_upserts_the_latest_observation_cache` | LOCALLY_VERIFIED | real end-to-end: a real telemetry POST upserts the real Postgres row (overwrite-not-duplicate proven with two submissions), publishes to real NATS | No |
| S4-10-005 | Step 10 | The gate only applies to SANDBOX_ACTIVE -> ENGAGING; every other transition passes through unmodified | `GATED_TRANSITION` guard | `test_gate_is_a_noop_for_transitions_other_than_sandbox_active_to_engaging`, `test_invalid_transition_still_raises_the_state_machines_own_error` | LOCALLY_VERIFIED | real Postgres: a MONITORING case advances to STEERING_PENDING through the same function untouched; a DESTROYED case still raises `InvalidTransitionError` | No |
| S4-10-006 | Step 10 | Actual live observation collection on a real Windows sandbox (the harness's OS-introspection side) | Not built in Step 10 — Step 9a's `run-fingerprint-harness.ps1` already proves the same collection code works at build time; a run-time equivalent PowerShell collector calling `SpiderServiceLogic.submit_fingerprint_snapshot` is the Windows-host integration lab work | none possible without a Windows host | LAB_VERIFICATION_REQUIRED | | Yes — Windows host with a live sandbox |

## Section index (updated live)

- [x] Bootstrap Gate
- [x] Stage 0 / Step 1
- [x] Stage 0 / Step 1b
- [x] Stage 0 / Step 2
- [x] Stage 0 / Step 3
- [x] Stage 1 / Step 4
- [x] Stage 1 / Step 4b
- [x] Stage 1 / Step 5
- [x] Stage 2 / Step 6
- [x] Stage 2 / Step 7
- [x] Stage 2 / Step 7b
- [x] Stage 3 / Step 8a
- [x] Stage 3 / Step 8b
- [x] Stage 3 / Step 8c
- [x] Stage 3 / Step 8d (design only — LAB_VERIFICATION_REQUIRED, no Windows host)
- [x] Stage 4 / Step 9a (structure LOCALLY_VERIFIED; live build/scan/SBOM/KMS-signing LAB_VERIFICATION_REQUIRED — no AWS account/Windows host)
- [x] Stage 4 / Step 9b (mechanism LOCALLY_VERIFIED end-to-end; restricted service account + real AWS reset/rebuild timing LAB_VERIFICATION_REQUIRED)
- [x] Stage 4 / Step 10 (gate mechanism LOCALLY_VERIFIED end-to-end; real Windows-host live observation collection LAB_VERIFICATION_REQUIRED)
- [x] Task #17 — CI/Docker/Makefile wiring (not a spec Step; mechanism LOCALLY_VERIFIED; live GitHub Actions run LAB_VERIFICATION_REQUIRED)

*(Stages 1–4 sections follow the same structure and are appended as each step
begins implementation — see the running list above for what has rows so far.)*

## Task #17 — CI/Docker/Makefile wiring (cross-cutting, not a spec Step)

| ID | Spec Section | Requirement | Implementation | Tests | Status | Evidence | Lab Dep. |
|---|---|---|---|---|---|---|---|
| T17-001 | "Docker Compose (single control node)" (locked tech) | Every application service (not just stateful infra) has a real, working Dockerfile | `services/{mirage-api,mirage-agent-ingestion,mirage-sandbox-gateway,mirage-worker,mirage-outbox-relay}/Dockerfile` | manual `docker build` + `docker run` for each, then full `docker compose up` | LOCALLY_VERIFIED | all 5 images build; all 5 containers reach real HEALTHY/running status against the real persistent dev stack (Postgres/NATS/Elasticsearch/Keycloak/step-ca) | No |
| T17-002 | Prompt-1 instructions (control files / correctness) | `pip install .` (non-editable, real install) actually works — not just `-e .` | `pyproject.toml` `packages`/`package-data` fixes | same Docker builds above; `python -c "from mirage_contracts.generated import ..."` inside the built image | LOCALLY_VERIFIED | 2 real packaging bugs found and fixed (ADR-0024 decisions 3) | No |
| T17-003 | Spec §6.5-adjacent: "CI validates golden fixtures for current + previous versions" | CI runs the drift/breaking-change contract check on every push/PR | `.github/workflows/ci.yml` `lint-typecheck-unit` job -> `make validate-contracts` | every individual command verified locally (see TEST_RESULTS.md) | CODE_COMPLETE | YAML-valid; every referenced command passes when run manually; the workflow has not executed inside a real GitHub Actions runner | Yes — real GitHub Actions run |
| T17-004 | Prompt-1 instructions | `make` targets referenced by the project actually work | `Makefile` (`test-fingerprint`, `test-steering`, `validate-config`, `scan-secrets`, `ci`, `docker-build`, `compose-*` targets fixed/added) | `make ci`, `make test-fingerprint`, `make lint` all run for real | LOCALLY_VERIFIED | `test-fingerprint` previously pointed at a nonexistent `tests/fingerprint` directory (0 tests, silently); now runs the real 28 fingerprint-related tests | No |
| T17-005 | Prompt-1 instructions | No secret values committed; the scanner covers the whole tree it claims to | ruff `extend-include`, `# secret-scan: ignore` marker on the new required compose variable | `make scan-secrets`, `pre-commit run --all-files` | LOCALLY_VERIFIED | `ruff check scripts` previously matched zero files (extensionless CLI tools); 8 real pre-existing style violations found and fixed once corrected | No |
## Prompt 2 — Stages 5 through 8

| ID | Stage/Step | Requirement | Implementation | Verification | Status | Lab dependency |
|---|---|---|---|---|---|---|
| P2-11-001 | Stage 5 / 11 | Immutable evidence ledger and exact object version | `0008_prompt2_stages_5_to_8`, `mirage_common.evidence` | unit + real PostgreSQL/MinIO integration | LOCALLY_VERIFIED | AWS enforcement |
| P2-11-002 | Stage 5 / 11 | Streaming acquisition hash, provenance, source sequencing/idempotency | `EvidenceService.acquire` | conflict/replay/unit/E2E tests | LOCALLY_VERIFIED | No |
| P2-11-003 | Stage 5 / 11 | Re-verification history and export eligibility | `EvidenceService.verify`, `export_eligibility` | missing/mismatch/gap tests | LOCALLY_VERIFIED | No |
| P2-11-004 | Stage 5 / 11 | Evidence lifecycle contracts and NATS routing | six new event schemas/generated models/subjects | contract, subject, real NATS outbox test | LOCALLY_VERIFIED | No |
| P2-11-005 | Stage 5 / 11 | Deterministic manifest/package and independent verification | `mirage_common.evidence_export`, report worker, verifier CLI | deterministic/tamper/E2E tests | LOCALLY_VERIFIED | No |
| P2-11-006 | Stage 5 / 11 | RSA-PSS KMS signing and trusted timestamp abstraction | `PSSSigner`, `KMSSigner`, `RFC3161TimestampAuthority` | local RSA/OpenSSL logic tests | LAB_VERIFICATION_REQUIRED | AWS KMS + real TSA |
| P2-12-001 | Stage 6 / 12 | Versioned rolling behavior and skill profile | `mirage_common.behaviour` | 3 unit tests + E2E | LOCALLY_VERIFIED | No |
| P2-13-001 | Stage 6 / 13 | Bounded deterministic AI snapshot with untrusted separation | `build_snapshot` | trimming/injection tests | LOCALLY_VERIFIED | No |
| P2-13-002 | Stage 6 / 13 | Strict versioned provider-independent proposal | `Proposal`, configured/fake/disabled adapters | schema/provider tests | LOCALLY_VERIFIED | Live provider |
| P2-13-003 | Stage 6 / 13 | Secrets, timeout/retry/circuit/fallback | secret resolver, `RefreshingSecret`, provider runtime | unit tests | LOCALLY_VERIFIED | Live Secrets Manager/provider |
| P2-13-004 | Stage 6 / 13 | Deterministic policy for every allowed action | `evaluate_policy` | all 10 policy action families tested | LOCALLY_VERIFIED | No |
| P2-13-005 | Stage 6 / 13 | Prompt-injection defenses | nested dangerous-key validation and untrusted snapshot section | 11 focused injection tests | LOCALLY_VERIFIED | No |
| P2-13-006 | Stage 6 / 13 | Persistent token/cost/request budgets | `ai_orchestration`, `ai_usage` | unit + real PostgreSQL E2E | LOCALLY_VERIFIED | Live provider billing comparison |
| P2-13-007 | Stage 6 / 13 | Exact AI OpenTelemetry metrics | `mirage_common.telemetry` | static/type/runtime construction tests | LOCALLY_VERIFIED | Collector backend |
| P2-14-001 | Stage 7 / 14 | Fail-closed multi-adapter scanner and archive controls | `mirage_common.artifacts`, scanner worker/image | unit + real scanner E2E | LOCALLY_VERIFIED | Production signature feeds |
| P2-14-002 | Stage 7 / 14 | Approved artifact deployment with bounded token/hash/root | Prompt 2 API + environment controller | unit + E2E | LOCALLY_VERIFIED | Windows surface |
| P2-14-003 | Stage 7 / 14 | Real revocation rollback state machine | API `ROLLBACK_PENDING`, controller journal, result reconciliation | direct PostgreSQL lifecycle test + controller integration | LOCALLY_VERIFIED | Windows surface |
| P2-15-001 | Stage 7 / 15 | Opaque canary lifecycle and safe classification | `mirage_common.canary`, callback API | 7 unit + E2E tests | LOCALLY_VERIFIED | Public callback |
| P2-15-002 | Stage 7 / 15 | Serverless public collector IaC | Terraform canary module + dev/acceptance roots | fmt + provider-schema validate | LAB_VERIFICATION_REQUIRED | AWS/DNS/TLS |
| P2-16-001 | Stage 8 / 16 | Versioned analyst directives and strategy linkage | `mirage_common.analyst`, worker strategy apply | unit + real PostgreSQL E2E | LOCALLY_VERIFIED | No |
| P2-17-002 | Stage 8 / 17 | Safe direct messages with preview/confirmation/policy/rates | analyst API/worker | unit + integration + E2E | LOCALLY_VERIFIED | Windows/decoy display surface |
| P2-17-003 | Stage 8 / 17 | Emergency disable/re-enable and delivery recheck | `analyst_channel_controls` and APIs | real PostgreSQL integration | LOCALLY_VERIFIED | No |
| P2-17-004 | Stage 8 / 17 | Delivered analyst interaction becomes evidence | delivery worker + `analyst_messages.evidence_id` | E2E | LOCALLY_VERIFIED | No |
