"""MirageEndpoint — the employee Windows endpoint telemetry service.

`__build_hash__` is a placeholder overwritten at MSI build time with the
release source/build identity from the signed release manifest. The frozen
binary and MSI have separate SHA-256 values recorded in that manifest.
"""

__build_hash__ = "0" * 64
