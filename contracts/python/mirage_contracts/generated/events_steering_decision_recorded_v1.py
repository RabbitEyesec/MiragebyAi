# generated by datamodel-codegen:
#   filename:  steering.decision_recorded.v1.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, conint, constr


class EventsSteeringDecisionRecordedV1(BaseModel):
    """
    Payload for event_type=steering.decision_recorded, schema_version major=1. Emitted whenever /route selects a backend (Step 8a, §6.1) and whenever a routing_decisions row is created/revoked via POST /api/v1/cases/{id}/steer[/revoke].
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    decision_id: constr(pattern=r'^[0-7][0-9A-HJKMNP-TV-Z]{25}$')
    case_id: constr(pattern=r'^[0-7][0-9A-HJKMNP-TV-Z]{25}$')
    protocol: Literal['HTTP', 'SSH', 'RDP']
    match_key: constr(min_length=1, max_length=512) = Field(
        ...,
        description='protocol + listener_id + source_ip + SNI_or_authenticated_principal, canonically joined.',
    )
    target: Literal['ENDPOINT', 'SANDBOX']
    decision_version: conint(ge=1)
    action: Literal['CREATED', 'SELECTED', 'REVOKED', 'EXPIRED', 'FAILSAFE_DEFAULT']


Model = EventsSteeringDecisionRecordedV1
