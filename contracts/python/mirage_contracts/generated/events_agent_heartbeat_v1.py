# generated by datamodel-codegen:
#   filename:  agent.heartbeat.v1.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, conint, constr


class EventsAgentHeartbeatV1(BaseModel):
    """
    Payload for event_type=agent.heartbeat, schema_version major=1. Used for GET /api/v1/agents and health rollups (Step 4b). queue_depth was added in 1.1 as an additive, optional field — a 1.0 instance omitting it still validates.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    agent_id: constr(min_length=1, max_length=256)
    role: Literal['ENDPOINT', 'SPIDER', 'ENV_CONTROLLER']
    build_hash: constr(pattern=r'^[a-f0-9]{64}$')
    version: constr(pattern=r'^[0-9]+\.[0-9]+\.[0-9]+$')
    certificate_serial: constr(min_length=1, max_length=128)
    uptime_seconds: conint(ge=0)
    health_state: Literal['HEALTHY', 'DEGRADED', 'UNHEALTHY']
    queue_depth: conint(ge=0) | None = Field(
        None,
        description='Added in schema_version 1.1 (additive/optional). Local encrypted-queue depth.',
    )


Model = EventsAgentHeartbeatV1
