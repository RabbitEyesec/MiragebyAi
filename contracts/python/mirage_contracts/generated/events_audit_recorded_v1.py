# generated by datamodel-codegen:
#   filename:  audit.recorded.v1.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, constr


class EventsAuditRecordedV1(BaseModel):
    """
    Payload for event_type=audit.recorded, schema_version major=1. Mirrors audit_events (Appendix B). Every state-changing transaction writes an audit row in the same DB transaction (Step 6); this event is its outbox-published counterpart.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    actor: constr(min_length=1, max_length=256)
    actor_type: Literal['ANALYST', 'AI', 'SYSTEM', 'AGENT']
    action: constr(min_length=1, max_length=256)
    target: constr(min_length=1, max_length=256)
    outcome: Literal['SUCCESS', 'FAILURE', 'DENIED']
    correlation_id: constr(pattern=r'^[0-7][0-9A-HJKMNP-TV-Z]{25}$')
    detail: constr(max_length=2048) | None = None


Model = EventsAuditRecordedV1
