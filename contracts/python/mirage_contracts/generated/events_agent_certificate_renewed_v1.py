# generated by datamodel-codegen:
#   filename:  agent.certificate_renewed.v1.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, confloat, constr


class EventsAgentCertificateRenewedV1(BaseModel):
    """
    Payload for event_type=agent.certificate_renewed, schema_version major=1. Audit trail requirement: renewal. Identity (agent_id) is preserved across renewal — old_certificate_serial and new_certificate_serial share the same agent_id.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    agent_id: constr(min_length=1, max_length=256)
    old_certificate_serial: constr(min_length=1, max_length=128)
    new_certificate_serial: constr(min_length=1, max_length=128)
    renewed_at_lifetime_fraction: confloat(ge=0.0, le=1.0) = Field(
        ...,
        description='Fraction of certificate lifetime remaining at renewal time; must be <= 0.20 per Step 3 auto-renew rule.',
    )


Model = EventsAgentCertificateRenewedV1
