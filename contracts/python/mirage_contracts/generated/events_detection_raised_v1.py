# generated by datamodel-codegen:
#   filename:  detection.raised.v1.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, RootModel, confloat, constr


class SourceRef(RootModel[constr(min_length=1, max_length=512)]):
    root: constr(min_length=1, max_length=512)


class EventsDetectionRaisedV1(BaseModel):
    """
    Payload for event_type=detection.raised, schema_version major=1. Emitted by the Elastic Defend / Suricata adapters (Step 7). The detection adapter never steers — this event carries no routing/target information.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    detector: Literal['ELASTIC_DEFEND', 'SURICATA']
    signature_id: constr(min_length=1, max_length=256)
    severity: Literal['LOW', 'MEDIUM', 'HIGH', 'CRITICAL']
    confidence: confloat(ge=0.0, le=1.0)
    correlation_key: constr(min_length=1, max_length=256) = Field(
        ...,
        description='Deterministic key used to correlate related detections into one case (host + rule family + time-window derived).',
    )
    source_refs: list[SourceRef] = Field(
        ...,
        description='References to the raw Elastic/Suricata source documents (index+doc id), never the raw payload itself.',
        max_length=20,
        min_length=1,
    )


Model = EventsDetectionRaisedV1
