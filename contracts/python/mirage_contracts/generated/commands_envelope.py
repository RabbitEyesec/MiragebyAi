# generated by datamodel-codegen:
#   filename:  envelope.schema.json

from __future__ import annotations

from typing import Any, Literal

from pydantic import BaseModel, ConfigDict, Field, conint, constr


class CommandsEnvelope(BaseModel):
    """
    The envelope wrapping every structured sandbox action (spec Appendix C). `params` is validated separately against the schema registered for `command_type`'s major schema_version. Model/AI output is NEVER a shell command — command_type is always one of a fixed, registered set (Appendix I; framework enforced here, concrete sandbox action types registered in Stage 4 / Step 9b).
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    command_id: constr(pattern=r'^[0-7][0-9A-HJKMNP-TV-Z]{25}$')
    command_type: constr(pattern=r'^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)+$') = Field(
        ...,
        description="Dot-namespaced command type from the fixed registered action set, e.g. 'sandbox.place_artifact'.",
    )
    schema_version: constr(pattern=r'^[0-9]+\.[0-9]+$')
    case_id: constr(pattern=r'^[0-7][0-9A-HJKMNP-TV-Z]{25}$')
    sandbox_id: constr(min_length=1, max_length=256)
    expected_state_version: conint(ge=0) = Field(
        ...,
        description='Optimistic-concurrency check against sandbox_instances.state_version; a mismatch rejects the command.',
    )
    issued_by: Literal['AI', 'ANALYST', 'SYSTEM']
    policy_decision_id: constr(pattern=r'^[0-7][0-9A-HJKMNP-TV-Z]{25}$')
    params: dict[str, Any] = Field(
        ...,
        description='command_type-specific parameters, validated against the registered params schema for (command_type, schema_version major).',
    )
    expires_at: constr(
        pattern=r'^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\.[0-9]{3}Z$'
    )


Model = CommandsEnvelope
