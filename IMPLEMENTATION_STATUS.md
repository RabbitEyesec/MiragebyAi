# Implementation Status

Updated continuously during the build. This is the fast-scan view; per-requirement
detail and evidence live in `REQUIREMENTS_TRACEABILITY.md`. Test evidence and exact
commands live in `TEST_RESULTS.md`.

Statuses: `NOT_STARTED` · `IN_PROGRESS` · `CODE_COMPLETE` · `LOCALLY_VERIFIED` ·
`LAB_VERIFICATION_REQUIRED` · `ACCEPTED` · `BLOCKED`

Scope: Prompts 1–3. Overall acceptance is not claimed because Profile B has
not run twice.

Acceptance count control: the local signed package has **71 total NOT_RUN**
rows, decomposed as **10 local synthetic numeric + 61 Profile B** (25 numeric
+ 36 scenario). Mirage remains `LAB_VERIFICATION_REQUIRED` and not
`ACCEPTED`.

## Prompt 3 scope table

| Area | Status | Notes |
|---|---|---|
| Stage 9 / Step 18 — dashboard, read model, realtime, 2D graph | LOCALLY_VERIFIED | Six Next.js workspaces, OIDC BFF, API RBAC/case grants, migration 0009, gap-aware projector/SSE, canonical Cytoscape 2D model, evidence pivots, directives, direct-message confirmation, responsive and browser tests |
| Stage 9 / Step 19 — 3D relationship graph | LOCALLY_VERIFIED | Three.js view consumes the same canonical nodes/edges as 2D; parity covers counts, IDs, relationships, classifications, overlays, evidence references, filters, timeline pivots, and browser interactions |
| Stage 10 / Step 20 — reports and export | LOCALLY_VERIFIED | Migration 0010, asynchronous report worker/API/UI, 27 classified sections, PDF/DOCX/JSON, signed canonical ZIP, independent verifier, cancellation/idempotency/progress, single-use downloads, real PostgreSQL integration |
| Stage 11 / Step 21 — hardened installers | LOCALLY_VERIFIED with lab gates | Transactional Ubuntu installer with protected recipes and signed report verifier, fresh Ubuntu 24.04 non-mutating preflight, hardened endpoint/sandbox WiX sources, and signed release/SBOM verifier. Mutating clean-host lifecycle and Windows compile/sign remain lab work |
| Stage 11 / Step 22 — failure, load, and security | LOCALLY_VERIFIED with lab gates | Thirteen definitions and bounded Profile B recipe runner, 31-check security plan, and reduced load/replay harness pass locally. Deployed fault injection, penetration/sign-off, and 1,000 eps/outage measurements remain lab work |
| Stage 11 / Step 22a — OpenTelemetry observability | LOCALLY_VERIFIED with lab gates | Exactly 40 core metric names, safe attributes, collector, 13 alert rules, and Elastic saved objects are verified structurally; deployed alert-delivery timing remains lab work |
| Stage 12 / Step 23 — teardown | LOCALLY_VERIFIED with AWS gate | Exact ordered 25-step workflow, dry-run/confirm/journal/resume/idempotence/gap override, local/AWS inventory adapters, real finalisation/drain/revocation scripts, exact tag inventory, local zero-resource and reconnect rejection tests. AWS destruction and inventory result are not run |
| Stage 12 / Step 24 — acceptance | LOCALLY_VERIFIED; LAB_VERIFICATION_REQUIRED | Machine-readable 25 numeric targets and 36 steps, named substitutions, signed JSON/HTML/PDF/DOCX package and hardened verifier, local composite harness, Profile B evidence ingestion and strict twice-run orchestration. Package-wide NOT_RUN is 71: 10 local numeric plus all 61 Profile B target/scenario rows |
| Prompt 3 CI/build wiring | LOCALLY_VERIFIED locally; hosted CI not run | Required Make targets and seven CI jobs cover dashboard, graphs, reports, installers, signed release/Ubuntu preflight, dependency audit, failure, security, reduced load, OTel, teardown, synthetic acceptance, and earlier regressions |

## Prompt 1 scope table

| Area | Status | Notes |
|---|---|---|
| External Bootstrap Gate | LOCALLY_VERIFIED | Config schema + 3 example configs + 5 scripts done; dev Compose stack (Postgres/NATS/Elastic/Keycloak/step-ca) up & healthy; secret scan has verified detection; acceptance bootstrap correctly gates on AWS (LAB_VERIFICATION_REQUIRED) |
| Stage 0 / Step 1 — Contracts & schema versioning | LOCALLY_VERIFIED | 18 JSON Schemas, generated Pydantic + TS models, 34 contract tests + 9 TS tests + 4 unit tests all pass; drift detection verified with a real injected-then-reverted schema change |
| Stage 0 / Step 1b — NATS JetStream streams | LOCALLY_VERIFIED | 6 streams provisioned + tested against a real container; 8 integration + 20 unit tests pass; dead-letter/replay/restart/dedup all proven for real |
| Stage 0 / Step 2 — AWS foundation (Terraform) | LOCALLY_VERIFIED (apply is LAB_VERIFICATION_REQUIRED) | VPC/IAM/evidence/log modules complete; validate+fmt+tfsec all clean; 16 static network-policy tests pass and are mutation-tested; `terraform apply`/`destroy` need a real AWS account |
| Stage 0 / Step 3 — Trust, enrolment, rotation (step-ca) | LOCALLY_VERIFIED | 5 certificate profiles provisioned; full enrol/renew/revoke lifecycle implemented + tested against real Postgres + real step-ca (8 integration + 2 unit tests); Secrets Manager loader is a documented stub (no AWS account to test against) |
| Stage 1 / Step 4 — Employee endpoint (dev MSI) | LOCALLY_VERIFIED (MSI compile + PS scripts LAB) | Full business logic (queue/sequencer/enroll/heartbeat) real + tested incl. real live-server E2E; real Elastic templates verified against real ES; WiX source + PowerShell scripts written, untestable without Windows |
| Stage 1 / Step 4b — Early engineering console | LOCALLY_VERIFIED | mirage-api (health/agents/events/cases + synthetic health-check), Keycloak OIDC auth (platform_admin-only), mirage-health ES data stream, CLI (`scripts/mirage-health`), Kibana dashboard — all real, 7/7 integration tests pass, dashboard import verified against a real Kibana container; no lab-only pieces |
| Stage 1 / Step 5 — MirageSpider | LOCALLY_VERIFIED (Windows service shim LAB) | Enrolls over mTLS, real telemetry ingestion endpoint (new, also closes Step 4's deferred gap), ordered+case-tagged observation events, tamper high-priority immediate-else-queue path, real Elastic data stream — 11 new tests pass against real Postgres/NATS/step-ca/Elasticsearch; win_service.py pywin32 shim is lab-only like Step 4's |
| Stage 2 / Step 6 — Case state machine + outbox | LOCALLY_VERIFIED | Full 10-state lifecycle, optimistic locking, transactional outbox (case.state_changed + audit.recorded per transition), mirage-outbox-relay (§6.3 polling relay) — 9 new tests pass against real Postgres/NATS; migration up/down/up verified against real dev DB; relay smoke-tested end-to-end against the real dev stack |
| Stage 2 / Step 7 — Detection into cases | LOCALLY_VERIFIED | Detection-into-cases adapter (dedup via processed_events, correlate via cases.correlation_key, publish case.created via outbox, never steers) — 5 new tests pass against real Postgres/NATS; migration 0004 up/down/up verified against real dev DB; correlation smoke-tested end-to-end against the real dev stack |
| Stage 2 / Step 7b — Dev sandbox target | LOCALLY_VERIFIED (RDP leg LAB) | Real HTTP (nginx) + real SSH (openssh-server, key auth) dev-sandbox containers, Spider reporting real telemetry about them, a real §6.5-structured fingerprint baseline — 7 new tests pass; compose stack manually verified end-to-end. RDP has no viable local server — LAB_VERIFICATION_REQUIRED |
| Stage 3 / Step 8a — Routing decision API | LOCALLY_VERIFIED | `routing_decisions` table (real Postgres EXCLUDE constraint), `/route` (mTLS-authenticated, 1s TTL cache) + `POST /steer` (platform_admin), steering.decision_recorded published on every write/resolution — 8 new tests pass against real Postgres/NATS/Keycloak; migration 0005 up/down/up verified against real dev DB |
| Stage 3 / Step 8b — HTTP broker | LOCALLY_VERIFIED | Real Nginx `auth_request` dynamic upstream selection via real mirage-api `/route` — 2 tests pass against real backend containers; 4 real nginx/Docker bugs found and fixed (see ADR-0020) |
| Stage 3 / Step 8c — SSH broker | LOCALLY_VERIFIED | Real OpenSSH bastion `ForceCommand` selector, real nested SSH sessions land in the correct backend — 2 tests pass; 2 more real bugs found and fixed (sshd_config Include, ForceCommand env sanitization — ADR-0020) |
| Stage 3 / Step 8d — RDP broker | CODE_COMPLETE (design doc; LAB_VERIFICATION_REQUIRED for everything else) | RD Gateway's backend selection is a compiled COM plugin (`IRDGPolicyEngine`), not a config file — `infra/broker/rdp/README.md` documents the real design rather than fabricating unusable code (no Windows host exists in this environment) |
| Stage 4 / Step 9a — Golden image (Packer) | LOCALLY_VERIFIED (structure; live build/scan/SBOM/KMS-sign LAB) | §6.5 comparator engine (`libs/mirage_common/fingerprint.py`, shared with Step 10) — 8/8 unit tests pass against the real Step 7b baseline; full Packer template (source + 8 provisioners + manifest post-processor) — 16/16 static-HCL structural tests pass (order, no public IP, WinRM/TLS, tags, no hardcoded secrets); `scripts/sign-ami-manifest` real boto3 KMS-signing code with a working `--dry-run` path; no AWS account/Windows host exists to run `packer build` itself |
| Stage 4 / Step 9b — Environment Controller | LOCALLY_VERIFIED (mechanism; restricted account + real AWS reset/rebuild timing LAB) | `sandbox_instances`/`sandbox_actions` tables (migration 0006, up/down/up verified against real dev DB); MirageEnvironmentController enrols via the ENV_CONTROLLER PKI identity reserved since Step 3; mirage-sandbox-gateway (real WSS command dispatch, optimistic concurrency, transactional outbox); a real structured action (PLACE_ARTIFACT) executes and rolls back with full Postgres audit; restricted-mutation-root policy enforced in code against real path-traversal attempts; every output tagged — 30 new unit tests + 5 new integration tests (real Postgres/NATS/step-ca/Keycloak + real WS controller client), all passing |
| Stage 4 / Step 10 — Fingerprint gate | LOCALLY_VERIFIED (mechanism; real Windows-host live observation collection LAB) | Live, blocking §6.5 gate wraps `transition_case` for SANDBOX_ACTIVE -> ENGAGING only; a new `spider.fingerprint_snapshot` telemetry event + Postgres "latest observation cache" table feed it; missing/failing MUST checks block for real with a durable audit trail even if the caller's transaction rolls back; 6 new integration + 3 new unit tests (gate) + 2 new unit + 1 new integration test (Spider submission path), all passing against real Postgres/NATS/step-ca |
| CI/Docker/Makefile wiring (Task #17, not a spec Step) | LOCALLY_VERIFIED (mechanism; live GitHub Actions run LAB) | Five real Dockerfiles (mirage-api/agent-ingestion/sandbox-gateway/worker/outbox-relay), all built AND run for real against the persistent dev stack — every service reaches a genuine HEALTHY status via `docker compose up`, all 10 containers together; found and fixed 2 real `pip install .` packaging bugs (missing `mirage_contracts.generated` in `packages`, missing `package-data` for bundled JSON schemas — both invisible under every editable-install test run this whole build), a real `--env-file` gap in `scripts/bootstrap-development`/Makefile, an unbuffered-stdout logging gap, a port-8000 collision, and a silent `ruff check scripts` coverage gap (8 real style violations fixed). `.github/workflows/ci.yml` + `.pre-commit-config.yaml` written and every individual command verified locally; the CI workflow itself has not run inside a real GitHub Actions runner |

This table is updated after each step is actually implemented and tested. See
`SESSION_HANDOFF.md` for the exact Stage 9 / Step 18 Prompt 3 continuation.

## Prompt 2 scope table

| Area | Status | Notes |
|---|---|---|
| Stage 5 / Step 11 — Evidence acquisition and immutable ledger | LOCALLY_VERIFIED (AWS S3/Object Lock LAB) | Migration 0008, exact-version object storage, streaming SHA-256, provenance, sequence idempotency/conflict detection, verification history, gaps, outbox events, and authenticated APIs; real PostgreSQL/NATS/MinIO tests pass |
| Stage 5 / Step 11 — Deterministic signed exports | LOCALLY_VERIFIED (AWS KMS/trusted RFC 3161 LAB) | Pre-export 100% re-verification, canonical manifest, deterministic ZIP, RSA-PSS signer abstraction, timestamp abstraction, report worker, and independent hardened verifier pass locally; local signing/time are explicitly non-production |
| Stage 6 / Step 12 — Behaviour profile and skill assessment | LOCALLY_VERIFIED | Deterministic rolling profile, confidence/sample bounds, skill assessment, profile version/history, and snapshot input are unit/E2E verified |
| Stage 6 / Step 13 — AI orchestration, policy, and injection defence | LOCALLY_VERIFIED (live provider LAB) | Bounded deterministic snapshots, untrusted-content separation, strict versioned proposal schema, provider/secret adapters, last-known-good secret, timeouts/retries/circuit/fallback, all action policies, injection defenses, persistent budgets/usage, and exact OpenTelemetry instruments |
| Stage 7 / Step 14 — Artifact pipeline | LOCALLY_VERIFIED (Windows surfaces LAB) | Quarantine streaming, SHA-256, file/ClamAV/YARA/OLE adapters, archive-bomb controls, fail-closed timeouts, approval/classification, single-use bounded download, local controller placement/hash check, and real rollback state machine; real Linux scanner image exercised in E2E |
| Stage 7 / Step 15 — Canary service | LOCALLY_VERIFIED (public AWS path LAB) | Opaque token hashes, expiry/revocation/replay, HMAC forwarding contract, trusted-proxy/source history, safe classification, evidence acquisition, authenticated APIs, and Terraform DNS/TLS/API Gateway/WAF/Lambda/DLQ/logging module; both environment roots validate |
| Stage 8 / Step 16 — Analyst guidance | LOCALLY_VERIFIED | Versioned directives, expiry/cancellation/acknowledgement, deterministic strategy phase linkage, idempotency, outbox events, audit, emergency controls, and authenticated APIs |
| Stage 8 / Step 17 — Direct analyst intervention | LOCALLY_VERIFIED | Message preview/hash/confirmation, policy/rate/idempotency/session checks, emergency control rechecks, output attribution, audit, and message evidence |
| Prompt 2 local end-to-end | LOCALLY_VERIFIED | One real local scenario uses PostgreSQL, NATS JetStream, MinIO versioning/Object Lock API, real scanner tools, deterministic fake AI, local RSA-PSS/local time, simulated non-Windows controller surface, deterministic export, and independent verification |
| Prompt 2 CI/build wiring | LOCALLY_VERIFIED (GitHub hosted run LAB) | Required Make targets, dependency-audit job, 7 service images, Compose scanner/report services, config/environment schema, 9 runbooks, and OpenTelemetry config are present; exact local results are in `TEST_RESULTS.md` |
